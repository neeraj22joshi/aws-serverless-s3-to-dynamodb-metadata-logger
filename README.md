# aws-serverless-s3-to-dynamodb-metadata-logger
Serverless pipeline that automatically logs S3 object metadata into DynamoDB using AWS Lambda (Python 3.10). Event-driven architecture built on AWS using S3 event triggers, IAM roles, DynamoDB, and CloudWatch logging.
